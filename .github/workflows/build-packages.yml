name: Build packages
run-name: Building ${{github.ref_name}} deployables
on:
  push:
    tags:
    - 'v**'
    branches-ignore:
    - '**'
jobs:


  desktop-bin:
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu, windows]
        # TODO: Replace with `plat_` eventually
        sys: [cli_crossterm, gui_softbuffer]
        include:
        - ext: ''
        - platform: windows
          ext: '.exe'
    name: Build desktop-bin-${{ matrix.sys }}-${{ matrix.platform }}
    runs-on: ${{ matrix.platform }}-latest
    steps:
    - uses: actions/cache@v3
      with:
        path: ~/.rustup/
        key: rustup-${{matrix.platform}}-stable
    - uses: actions/cache@v3
      with:
        path: ~/.cargo/registry
        key: cargo-cache
    - uses: actions/cache@v3
      with:
        path: ./target
        key: target-dir-${{matrix.platform}}-stable
    - name: Set up Rust
      run: |
        rustup default stable

    - uses: actions/checkout@v3
    - name: Generate artifact
      run: |
        cargo build --release --features io_${{ matrix.sys }},run_rayon --bins

    - uses: actions/upload-artifact@v3
      with:
        name: desktop-bin-${{ matrix.sys }}-${{ matrix.platform }}
        path: |
          target/release/redshell${{ matrix.ext }}
        if-no-files-found: error
